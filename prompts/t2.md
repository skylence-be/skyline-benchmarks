# Task T2 — Repo-wide structural rewrite

Rewrite all calls to `track::event(a, b)` → `track::event_v2(a, b, track::Flags::default())`.

Rules:
- Transform every `track::event(` call (adding the third argument).
- Do NOT transform `track::event_id(...)` calls.
- Do NOT transform local `fn event(...)` function definitions.
- Do NOT change `track::event(` in comments or string literals.

When complete, output exactly: DONE-{CELL_ID}
