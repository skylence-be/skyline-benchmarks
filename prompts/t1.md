# Task T1 — Multi-site semantic rename

The free function `normalize_path` must be renamed to `canonicalize_path` throughout this codebase.

Rules:
- Rename the function DEFINITION and all FREE-FUNCTION call sites.
- Do NOT rename the method `UrlCanon::normalize_path` or calls like `.normalize_path(...)`.
- Do NOT change string literals or comments containing `normalize_path`.
- Do NOT change test function names like `test_normalize_path`.

When complete, output exactly: DONE-{CELL_ID}
