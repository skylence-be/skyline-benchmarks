# Task T3 — Hash-guarded precision edit in large files

In each file under `src/limits/` (8 files total), there are environment config modules:
`pub mod dev`, `pub mod staging`, and `pub mod prod`.

Change `MAX_RETRIES` to `7` in the `pub mod prod` block ONLY in every limits config file.

Rules:
- Change ONLY the value inside `pub mod prod { ... }`.
- Do NOT change `MAX_RETRIES` in `pub mod dev` or `pub mod staging`.

When complete, output exactly: DONE-{CELL_ID}
